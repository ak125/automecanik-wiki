{
  "name": "automecanik-wiki",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AutoMecanik wiki — base de connaissance canonique. Tooling Node-only (validation frontmatter via ajv).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate-frontmatter": "node _scripts/validate-frontmatter.mjs",
    "check-slug-uniqueness": "node _scripts/check-slug-uniqueness.mjs",
    "check-extension-whitelist": "node _scripts/check-extension-whitelist.mjs",
    "check-aec-hash": "bash _scripts/check-aec-hash.sh",
    "lint": "npm run validate-frontmatter && npm run check-slug-uniqueness && npm run check-extension-whitelist"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.1.0"
  }
}
